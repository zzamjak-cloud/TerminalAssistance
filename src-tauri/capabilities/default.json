{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 윈도우 기본 권한 (invoke + event + asset 프로토콜) — main-r* 는 크래시 복구로 재생성된 창",
  "windows": [
    "main",
    "main-r*"
  ],
  "permissions": [
    "core:default",
    "notification:allow-is-permission-granted"
  ]
}