# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.

## [0.13.0] - 2026-09-01

### 추가
- **탐색기 파일 편집기** — 미리보기 팝업의 `편집` 버튼이나 파일 우클릭 → `편집하기` 로 파일을 그 자리에서 고칠 수 있습니다. 확장자에 따라 세 가지 형태로 열립니다: 마크다운은 왼쪽 실시간 렌더 + 오른쪽 작성 2단, JSON 은 문법 검사와 `정렬` 버튼이 있는 편집기(문법이 깨진 상태로는 저장을 막습니다), 그 밖의 텍스트는 줄번호와 구문 강조가 붙은 코드 편집기입니다. 저장은 임시 파일 교체 방식이라 중간에 깨진 파일이 남지 않으며, `Cmd/Ctrl+S` 로도 저장됩니다. 저장하지 않은 변경이 있을 때 취소·`Esc` 는 한 번 더 확인을 받습니다. 2MB 가 넘는 파일은 잘린 내용으로 덮어쓰는 사고를 막기 위해 편집할 수 없습니다.
- **탐색기 새 파일·폴더 만들기** — 헤더 새로고침 버튼 오른쪽의 `＋` 로 선택한 폴더 안에 파일이나 폴더를 만듭니다. 파일은 이름에 넣은 확장자(`.md`, `.html`, `.js` …)에 맞는 편집기가 곧바로 열립니다.
- **탐색기 파일 우클릭 메뉴** — `미리보기`, `편집하기`, `삭제하기` 를 제공합니다. 삭제는 되돌릴 수 없으므로 같은 자리에서 `삭제 확인` 으로 한 번 더 받습니다.
- **탐색기 드래그 이동** — 파일·폴더를 다른 폴더 행 위로 끌어 옮길 수 있습니다. 빈 영역에 놓으면 프로젝트 루트로 이동하며, 대상에 같은 이름이 있거나 폴더를 자기 하위로 옮기려 하면 거부하고 사유를 알립니다.
- **탐색기 `F2` 이름 변경** — 선택한 항목의 이름을 트리 안에서 바로 고칩니다. 확장자를 뺀 부분이 선택된 채로 시작하고, `Enter` 또는 바깥 클릭은 저장, `Esc` 는 취소입니다.
- **패널 제목 드롭다운 검색** — 드롭다운 최상단에 검색창이 생겼습니다. 프로젝트 이름이 걸리면 그 프로젝트의 세션 전체와 `+ 새 세션` 항목이 함께 남고, 세션이 없는 프로젝트도 이름만 맞으면 나옵니다. 정확히 치지 않아도 되는 느슨한 매칭이라 `trmasi` 로 `TerminalAssistance` 를 찾을 수 있고, 공백으로 나눈 여러 단어는 모두 걸려야 합니다. `Enter` 는 첫 세션으로 전환하고, 세션이 없으면 그 프로젝트에 새 세션을 엽니다.
- **빈 패널 세션 선택 화면 검색** — 분할 화면의 빈 패널에도 같은 방식의 검색창이 `표시할 세션 선택` 제목 아래에 붙었습니다. 목록이 다시 그려져도 치던 검색어와 포커스는 유지됩니다.

### 개선
- **탐색기 제목 정리** — 사이드바에 이미 선택한 프로젝트가 드러나므로 탐색기 제목은 `탐색기` 로 고정하고, 전체 경로는 툴팁으로 옮겼습니다.
- **편집 팝업 크기** — 편집기는 화면을 최대한 쓰는 크기(96vw × 94vh)로 열립니다.

## [0.12.10] - 2026-09-01

### 추가
- **`Cmd/Ctrl+J` — 터미널 ↔ 프롬프트 입력창 커서 이동** 현재 포커스된 패널에서 터미널 본문과 프롬프트 입력창 사이로 커서를 옮깁니다. 사이드바·탐색기 등 다른 곳에 있을 때는 입력창으로 들여보냅니다. 분할 화면의 패널 순회(`Tab`)와 터미널 안의 `Shift+Tab`(Claude Code 권한 모드 전환)은 그대로 유지됩니다.
- **터미널에 잘못 치던 프롬프트 잘라내 오기** — 프롬프트를 터미널 입력창에 치고 있었다면 `Cmd/Ctrl+J` 가 그 내용을 터미널에서 지우고 프롬프트 입력창 커서 위치로 옮겨 줍니다. 셸의 입력 버퍼는 앱이 읽을 수 없으므로 PTY 로 나가는 사용자 입력(한글 IME 커밋·붙여넣기 포함)을 추적해 글자 수만큼 지우며, 지우기 직전에 화면과 대조해 확인이 되지 않으면 터미널 내용을 건드리지 않고 커서만 옮기고 그 사유를 알립니다. 방향키·`Tab` 완성처럼 입력 라인을 따라갈 수 없는 조작 뒤에도 마찬가지로 그대로 둡니다.

## [0.12.9] - 2026-09-01

### 추가
- **패널 헤더 Pull 버튼** — 프리셋 드롭다운 왼쪽에 Pull 버튼이 생겼습니다. git 저장소인 세션에서만 표시되고, 저장소가 아니면 버튼 자체가 나타나지 않습니다. 세션을 시작할 때 한 번 `git fetch` 로 원격 상태를 확인해, 최신이면 버튼이 비활성화되고 받을 커밋이 있으면 초록색으로 활성화되며 우측 상단 원형 배지에 받아야 할 커밋 수가 표시됩니다. 클릭하면 터미널에 명령을 흘려보내지 않고 백그라운드에서 `git pull --ff-only` 를 실행한 뒤 결과를 토스트로 알려주므로, AI 에이전트가 돌고 있는 패널에서도 프롬프트를 방해하지 않습니다. 업스트림이 없거나 분기되어 fast-forward 가 불가능한 경우에는 그 사유를 툴팁·토스트로 안내합니다.

## [0.12.8] - 2026-08-31

### 수정
- **OSC 8 하이퍼링크 클릭이 먹통이던 문제** — Codex 등 TUI 가 OSC 8 이스케이프로 출력하는 링크는 xterm 내장 링크 제공자가 처리하는데, 앱이 `linkHandler` 를 주지 않아 밑줄만 생기고 클릭은 웹뷰에서 동작하지 않는 내장 폴백으로 새어나갔습니다. 이제 본문에 그냥 찍힌 URL 과 동일하게 시스템 기본 브라우저로 엽니다 (http/https 만 허용).
- **탐색기 Enter 가 프롬프트 입력창 대신 터미널로 경로를 넣던 문제** — 파일을 선택하고 Enter 를 누르면 활성 화면의 프롬프트 입력창 커서 위치에 경로가 들어갑니다. 입력창이 없거나 잠긴 경우에만 기존처럼 터미널 입력 라인에 넣습니다. 포커스는 트리에 남아 여러 파일을 연달아 넣을 수 있습니다.

## [0.12.7] - 2026-08-30

### 수정
- **터미널 파일 경로 링크 인식 보강** — TUI 가 화면 폭에 맞춰 스스로 끊어 출력한 긴 경로를 이어서 한 링크로 인식하고, 절대 경로의 선행 슬래시가 잘려 들어가던 문제를 고쳤습니다.

## [0.12.6] - 2026-08-29

### 추가 및 개선
- **패널 제목 바 드래그로 위치 변경** — 분할 화면에서 헤더의 제목 바를 끌어 패널 순서를 바꿀 수 있습니다. 드롭 지점은 파란 점선으로 표시되며(좌우 분할은 세로선, 상하 분할은 가로선), 놓으면 그 자리로 이동하고 사이 패널은 한 칸씩 밀립니다. 세션이 없는 빈 패널도 대상입니다.

### 수정
- **드롭다운으로 새 세션 시작 시 다른 패널 침범** — 빈 패널이 없을 때 제목 드롭다운에서 새 세션을 만들면 포커스 패널의 세션까지 새 세션으로 바뀌어 같은 세션이 두 패널에 뜨던 문제를 고쳤습니다.

## [0.12.5] - 2026-08-29

### 수정
- **Codex 줄바꿈 프롬프트 즉시 실행** — 줄바꿈이 포함된 프롬프트를 전송할 때 보내던 ESC+Enter 조합이 TUI에서 Alt+Enter(줄바꿈 삽입)로 해석돼 입력만 남고 대기하던 문제를 고쳤습니다. 붙여넣기와 분리된 Enter를 보내 전송 버튼이 항상 즉시 실행을 의미하도록 했습니다. 전송 직전 끝 공백 제거 범위도 전각 공백·NBSP까지 넓혔습니다.

### 개선
- **세션 드롭다운에 상시 새 세션 항목** — 패널 헤더 제목 드롭다운에서 이미 세션이 있는 프로젝트도 목록 아래의 `+ 새 세션 시작`으로 세션을 추가할 수 있습니다. 기존에는 세션이 없는 프로젝트에서만 보였습니다.

## [0.12.4] - 2026-08-28

### 수정
- **빈 프로젝트 선택 시 세션 즉시 생성** — 터미널 상단/분할 패널의 프로젝트 선택 목록에서 아직 세션이 없는 프로젝트를 고르면 새 세션을 만들고 해당 패널에 바로 표시합니다.
- **Codex 줄바꿈 프롬프트 전송 안정화** — 하단 프롬프트 입력창의 전송 버튼 또는 Cmd/Ctrl+Enter 전송 시 마지막 공백과 줄바꿈을 제거한 텍스트를 보내 Codex가 입력 대기 상태에 남는 상황을 줄였습니다.

## [0.12.3] - 2026-08-27

### 개선
- **macOS 알림 아이콘 보정** — 데스크톱 알림에 현재 실행 중인 앱 번들의 `icon.icns`를 직접 지정해 구버전 앱 등록/캐시가 남아 있어도 최신 아이콘을 쓰도록 했습니다.
- **프롬프트 입력창 기본 높이 확대** — 하단 프롬프트 입력창을 기본 3라인 크기로 키워 긴 지시를 작성할 때 초기 공간을 넓혔습니다.
- **프로젝트·세션 드롭다운 전환** — 패널 헤더의 프로젝트/세션 제목을 클릭하면 프로젝트별 세션 목록과 새 세션 시작 메뉴가 열립니다. 잘 쓰지 않던 세션 제목 인라인 변경 기능은 제거했습니다.

## [0.12.2] - 2026-08-27

### 수정
- **알림 클릭 시 앱 열기** — 데스크톱 알림을 클릭하면 창이 복원·포커스되고 알림을 띄운 세션이 화면에 올라옵니다. 기존에는 알림을 눌러도 아무 반응이 없었습니다(알림 플러그인이 클릭 콜백을 제공하지 않아 처리 자체가 없었음). Windows 는 토스트 Activated 이벤트, macOS 는 알림 클릭 응답을 직접 받아 처리합니다

## [0.12.1] - 2026-08-27

### 수정
- **프롬프트 입력창 Ctrl+P 인쇄 방지** — 입력창 포커스 중 Cmd/Ctrl+P가 브라우저 인쇄로 새지 않고 패널 토글만 수행합니다.
- **참조 이미지 스트립 접기** — 세션별 참조 이미지 표시 영역을 접고 펼칠 수 있으며 접힘 상태를 유지합니다.
- **Codex 줄바꿈 프롬프트 즉시 실행** — 줄바꿈이 포함된 프롬프트도 전송 버튼 클릭 시 Codex 입력 대기 상태에 남지 않고 실행을 시작하도록 보정했습니다.
- **드래그 선택 후 우클릭 선택 유지** — 선택 영역이 있는 상태의 우클릭 입력을 xterm보다 먼저 차단해 컨텍스트 메뉴를 열 때 선택이 해제되지 않도록 했습니다.

## [0.12.0] - 2026-08-27

### 추가 및 개선
- **터미널 파일 링크 미리보기** — 터미널 출력에 나타나는 파일 경로(`src/app.js:123` 같은 상대·절대 경로는 물론 `terminal-view.js` 처럼 경로 없이 언급된 파일명까지)에 마우스를 올리면 밑줄이 생기고, 클릭하면 탐색기와 같은 미리보기 팝업이 열립니다. 단독 파일명은 프로젝트 안을 검색해 실제 위치를 찾아 열며, 한글 파일명과 줄바꿈으로 감긴 긴 경로도 인식합니다. URL 링크는 기존처럼 브라우저로 열립니다
- **드래그 선택 자동 스크롤** — 텍스트를 드래그로 선택하다 포인터가 터미널 위/아래 경계를 벗어나면 벗어난 거리에 비례한 속도로 자동 스크롤되며 선택이 계속 확장됩니다. 화면 밖까지 이어지는 긴 내용도 한 번의 드래그로 선택할 수 있습니다
- **Shift+클릭 확장 선택** — 시작 지점을 클릭해 두고 스크롤한 뒤 끝 지점에서 Shift+클릭하면 두 지점 사이가 한 번에 선택됩니다. 아주 긴 내용을 복사할 때 드래그 대신 쓸 수 있습니다
- **Ctrl+C 복사 시 선택 유지** — 선택된 텍스트가 있을 때 Ctrl+C 는 복사만 하고 선택을 유지합니다(색상 보존 복사). 선택이 없을 때만 기존처럼 실행 중인 작업을 중단(^C)합니다
- **우클릭 메뉴: 복사 · 메모에 등록하기** — 텍스트 선택 후 우클릭하면 컨텍스트 메뉴가 열립니다. `메모에 등록하기`를 누르면 선택한 내용이 본문에 채워진 메모 작성 팝업이 뜨고 커서는 제목 칸에 위치합니다
- **메모 수정** — 문서 목록에서 메모를 클릭하면 열람 대신 수정 팝업이 열려 제목과 본문(Markdown 원문)을 자유롭게 고칠 수 있습니다. 생성 시각은 보존되고 수정 시각만 갱신됩니다
- **중간(휠) 클릭 팬 스크롤** — 휠 버튼 클릭으로 팬 스크롤을 켜고(커서가 ↕ 로 바뀜) 포인터를 위/아래로 움직이면 거리에 비례한 속도로 스크롤됩니다. 다시 휠 클릭하면 꺼지는 토글 방식이며, 다른 버튼 클릭이나 휠 굴림으로도 꺼집니다
- **Shift+Enter 예약 발송** — 프롬프트 입력창에서 Shift+Enter 를 누르면 예약 버튼과 동일하게 동작합니다(진행 중이면 완료 후 전송)
- **예약 취소 시 내용 복원** — 예약된 프롬프트의 취소 버튼을 누르면 내용이 입력창으로 되돌아와 바로 수정·추가할 수 있습니다. 입력창에 작성 중이던 텍스트가 있으면 지우지 않고 뒤에 이어붙입니다
- **터미널 스크롤바 개선** — 평소에는 숨겨져 있다가 터미널에 마우스를 올리면 가는 스크롤바가 나타납니다. 폭도 기존보다 좁아졌습니다

### 수정
- **마우스만 움직여도 선택이 사라지던 문제** — AI 도구(TUI)가 마우스 트래킹을 켜면 마우스 이동·우클릭 리포트가 "사용자 입력"으로 집계돼 선택이 즉시 지워졌습니다. 마우스 리포트를 사용자 입력에서 제외해 드래그·Shift+클릭으로 만든 선택이 유지됩니다(일반 클릭·키보드 입력 시 해제되는 표준 동작은 그대로)
- **포커스 없는 터미널의 선택 색이 다르게 보이던 문제** — 비활성 선택 색을 활성 선택 색과 동일하게 맞춰 어디에 포커스가 있든 선택 영역이 같은 색으로 보입니다
- **마우스 트래킹 중 휠 스크롤이 먹지 않던 문제** — TUI 가 마우스 트래킹을 켠 상태에서도 일반 버퍼의 스크롤백은 휠로 볼 수 있습니다

## [0.11.13] - 2026-08-26

### 수정
- **작업 중인데 완료 알림이 뜨던 문제** — 완료 판정이 "800ms 무출력" 만 보고 있어서, 도구 실행 중 경과 시간을 1초 간격으로 다시 그리는 구간이 매번 완료로 잡혔습니다. 그래서 세션이 끝나지 않았는데도 알림이 반복해서 떴습니다. 이제 Claude Code 훅이 설치된 세션은 `Stop` 훅 신호만을 완료 근거로 삼고(도구 대기 공백은 계속 '실행 중'), 같은 신호로 두 번 알리지 않습니다. 훅이 없는 세션(codex·셸)은 무출력 판정을 5초로 늘렸고, 훅 신호가 유실된 경우에는 근거 없는 완료 알림 대신 조용히 대기 상태로 내려갑니다. 완료 알림에 표시되는 소요 시간도 작업 전체 구간으로 계산됩니다
- **알림 팝업에 옛 앱 아이콘이 표시되던 문제** — macOS 알림 센터는 번들 ID 로 아이콘을 찾는데, 빌드 산출물(`src-tauri/target/*/bundle`)과 언마운트된 dmg 사본이 같은 번들 ID 로 LaunchServices 에 남아 그중 오래된 번들의 아이콘이 쓰이고 있었습니다. 낡은 등록을 정리하고 아이콘 캐시를 갱신하는 `scripts/macos-refresh-icon.sh` 를 추가했습니다

## [0.11.12] - 2026-08-26

### 추가 및 개선
- **Tab 으로 분할 패널 이동** — 분할 화면에서 프롬프트 입력창에 커서가 있을 때 Tab 을 누르면 다음 패널로 순서대로 이동하며 그 패널의 입력창에 커서가 들어갑니다(Shift+Tab 은 역순). 세션이 없는 패널은 건너뜁니다. 터미널 안에서 누르는 Tab 은 셸 자동완성 그대로입니다

### 변경
- **앱 버전 표기 위치** — 사이드바 헤더에 있던 버전 표기를 창 제목으로 옮겼습니다. macOS·Windows 모두 창 상단 제목 줄에 `Terminal Assistance v0.0.0` 으로 표시됩니다
- **우측 패널 헤더 한 줄 축소** — 접기 버튼(⟩)만 있던 패널 최상단 헤더 줄을 없애고 버튼을 `세션` 섹션 헤더로 합쳤습니다. 목록에 쓸 수 있는 세로 공간이 그만큼 늘어납니다

### 수정
- **분할 화면에서 세션이 앞 셀부터 채워지지 않던 문제** — 프로젝트를 선택해 세션이 시작될 때 포커스가 있던 패널에 배정돼, 셀이 모두 비어 있어도 가운데 셀에서 시작되는 경우가 있었습니다. 이제 항상 1번 셀부터 확인해 첫 번째 빈 셀에 배정하며, 빈 셀이 없을 때만 포커스 패널의 세션을 교체합니다
- **설정(⚙) 아이콘이 유독 작아 보이던 문제** — ⚙ 문자가 다른 아이콘보다 작게 그려져 상단바에서 눈에 띄게 작아 보이던 것을 글리프 크기를 키워 맞췄습니다(버튼 높이는 그대로)

## [0.11.11] - 2026-08-26

### 추가 및 개선
- **터미널 글꼴 변경** — 설정에 글꼴 항목이 생겼습니다. 시스템에 설치된 코딩 글꼴을 자동 감지해 드롭다운으로 보여주고(각 항목은 해당 글꼴로 미리 표시), `파일에서 선택`으로 시스템 글꼴 폴더의 글꼴 파일(ttf/otf/ttc)을 직접 골라 지정할 수도 있습니다(파일의 실제 패밀리 이름을 자동 추출). 선택 즉시 열려 있는 모든 터미널에 미리 적용되고, 취소하거나 Esc 로 닫으면 원래 글꼴로 되돌아가며, 저장하면 재시작·업데이트 후에도 유지됩니다. 지정 글꼴이 그리지 못하는 문자는 기본 글꼴로 대체됩니다

### 변경
- **설정 버튼 위치** — 사이드바 상단의 ⚙ 버튼을 상단바 우측(분할 버튼 좌측)으로 옮겼습니다. 사이드바를 접은 상태에서도 설정을 열 수 있습니다

### 수정
- **가독성 설정이 실제 적용되지 않던 문제** — 렌더러가 전역 App 존재를 `window.App` 로 확인해 항상 실패하는 바람에(최상위 `const` 는 window 에 붙지 않음) 줄 간격·자간·최소 대비 설정이 터미널에 반영되지 않고 기본값으로 남던 문제를 고쳤습니다. 테마 변경 시 프로젝트 색 갱신이 조용히 생략되던 것도 같은 원인으로 함께 수정됐습니다

## [0.11.10] - 2026-08-26

### 변경
- **상단바 전체 폭 고정** — 상단바가 사이드바 위까지 창 전체 폭을 차지하며 항상 최상단에 고정됩니다. 좌측에 프로젝트 사이드바·탐색기 토글 버튼이 새로 생기고, 우측 패널 토글(☰)도 같은 스타일의 아이콘 버튼으로 통일됐습니다. 각 버튼은 해당 패널이 열려 있으면 파랑으로 강조됩니다
- **사이드바 접기 방식 통일** — 프로젝트 사이드바·탐색기를 접었을 때 남던 슬림 레일(세로 컬럼 라인 2개)이 사라지고, 우측 패널과 동일하게 완전히 숨겨집니다. 상단바 토글 버튼, 패널 내부 접기 버튼(⟨), 기존 단축키(Cmd/Ctrl+I·O)로 여닫을 수 있으며 접힘 상태는 이전처럼 재시작 후에도 기억됩니다

## [0.11.9] - 2026-08-26

### 추가 및 개선
- **Git Bash 지원과 셸 자동 감지** — 설정의 셸 항목이 수동 입력에서 자동 감지 드롭다운으로 바뀌었습니다. 설치된 셸만 표시되며(Windows: PowerShell·PowerShell 7·cmd·Git Bash, macOS/Linux: zsh·bash), Git Bash 를 선택하면 표준 설치 경로와 PATH 의 git.exe 위치에서 실제 셸(`bin\bash.exe`)을 찾아 로그인 셸로 실행합니다. GUI 런처(git-bash.exe)를 지정해도 실제 셸로 바꿔 실행하고, 로그인 셸이 홈으로 이동하지 않고 프로젝트 폴더에서 시작하도록 처리했습니다. 이전 버전에서 직접 입력해 둔 값은 보존됩니다
- **크래시 자동 복구 확장 — GPU·브라우저 프로세스** — 렌더러 크래시만 자동 리로드하던 것을 넘어, GPU 프로세스 크래시(화면이 깨진 채 남던 증상)는 자동 리로드로, WebView2 브라우저 프로세스 크래시(창이 사라져 앱을 재시작해야 했던 증상)는 같은 프로세스 안에서 창을 재생성해 복구합니다. PTY 와 실행 중이던 작업(Claude/Codex 포함)은 그대로 살아 있어 세션을 수동으로 이어서 시작할 필요가 없습니다. 복구 조치는 `crash-recovery.log` 에 종류·시각과 함께 기록됩니다

### 수정
- **터미널 입력 중 커서를 프롬프트가 뺏어가던 문제** — 작업 완료 시 커서를 프롬프트 입력창으로 옮기는 동작이, 사용자가 터미널에서 직접 타이핑하는 중에도(타이핑 에코만으로 완료 판정이 생겨) 포커스를 가로챘습니다. 이제 최근 3초 안에 터미널에 직접 키 입력이 있으면 커서를 옮기지 않습니다. 한글 조합 중 포커스 이동으로 조합이 깨지는 경우도 함께 막았습니다

## [0.11.8] - 2026-08-26

### 추가 및 개선
- **패널 헤더·프리셋 드롭다운 통합** — 상단 고정 프리셋바를 없애고, 각 패널 헤더(단일 화면 포함)에 우측 정렬된 오렌지색 프리셋 드롭다운을 붙였습니다. 드롭다운 하나에 전역 프리셋(파랑, 맨앞) + 그 세션 프로젝트 전용 프리셋 + `+ 프리셋 추가` 메뉴가 모두 들어가며, 실행 대상은 그 패널의 세션입니다. 헤더 라인은 배경·구분선으로 터미널과 시각적으로 분리됩니다
- **패널 헤더에서 세션 이름 변경** — 헤더 제목에 마우스를 올리면 흐린 입력필드 힌트가 보이고, 더블클릭으로 인라인 수정할 수 있습니다(Enter/바깥 클릭 = 저장, Esc = 취소). 사이드바를 접어 둔 상태에서도 이름을 바꿀 수 있습니다
- **좌우 4분할 모드** — 분할 드롭다운에 4×1 이 추가됐습니다(세로 스플리터 3개, 열 비율은 트랙 수별로 따로 기억)
- **빈 분할 패널의 `+ 세션 추가`** — 빈 패널 피커에서 프로젝트를 골라 바로 그 패널에 새 세션을 열 수 있고, 세션이 하나도 없어도 분할화면을 쓸 수 있습니다. 신규 세션은 빈 패널부터 채우고 그 패널이 선택됩니다
- **패널 토글 단축키** — Cmd/Ctrl+P(우측 패널), Cmd/Ctrl+I(프로젝트 사이드바), Cmd/Ctrl+O(탐색기) 폴딩 토글
- **사이드바 개편** — `+ 프로젝트` 버튼을 상단 헤더(설정 좌측)의 ＋ 아이콘으로 옮기고, `일반 터미널`을 목록 최상단 고정 프로젝트로 승격했습니다(＋ 로 홈 디렉토리 세션 추가, 접기/미니 상태 점 지원). 하단 `+ 터미널` 버튼은 제거됐습니다
- **크래시 복구 안정화** — 작성 중이던 프롬프트 텍스트를 세션별로 영속화해 리로드 복구·앱 재시작 후에도 복원합니다(키 입력 프레임 밖 유휴 시간에 저장해 타이핑 버벅임 없음). 복구가 일어나면 하단 토스트로 알리고 누적 횟수·시각을 기록하며, WebView2 프로세스 실패 종류를 `crash-recovery.log` 에 남겨 원인 추적이 가능합니다
- **새 앱 아이콘** — 새 디자인(TA.png) 기반으로 전 플랫폼 아이콘(ico/icns/PNG/스토어/android/iOS)을 재생성했습니다

### 변경
- **프롬프트 입력창 시인성** — 입력창 배경을 터미널보다 밝은 회색으로 바꿔 터미널에 잘못 입력하는 일을 줄였고, 작업이 완료되면 커서가 그 패널의 입력창으로 자동 이동합니다(다른 입력 요소 작성 중에는 이동하지 않음)
- **Windows 편집 키를 Mac 과 통일** — 입력창에서 Ctrl+←/→ = 줄 시작/끝, Alt+←/→ = 단어 이동, Ctrl+Backspace = 줄 시작까지 삭제, Alt+Backspace = 단어 삭제(Shift 조합은 선택 확장, 실행 취소 이력 보존)
- **터미널 제어 조합키 차단** — Ctrl+D(셸 종료)·Ctrl+Z(중지)·Ctrl+S(출력 정지)·Ctrl+R(역검색) 등 터미널 제어 조합이 실수로 셸에 전달되지 않습니다. 실행 중단용 Ctrl+C 는 유지됩니다

### 수정
- **세션 첫 실행 시 입력창이 쪼그라드는 문제** — 숨김 상태에서 계산된 높이가 박제되던 원인을 고쳐 처음부터 기본 크기로 표시됩니다
- **예약 목록이 터미널에 가려지는 문제** — 예약 추가/취소로 목록 높이가 바뀌면 터미널을 다시 맞춥니다
- **패널 헤더 이름 편집에서 빠져나올 수 없던 문제** — Enter/바깥 클릭/Esc 모두 정상 동작합니다

## [0.11.7] - 2026-08-25

### 추가 및 개선
- **분할 모드 8종과 드롭다운 전환** — 상단바의 분할 버튼 4개를 드롭다운 하나로 합치고 1×1, 2×1(좌우 2), 1×2(상하 2), 2×2, 3×1(좌우 3), 1×3(상하 3), 3×2, 2×3 을 제공합니다(최대 6분할). 목록의 각 항목은 열·행 구성을 그대로 그린 아이콘으로 표시되고, 버튼 아이콘은 현재 모드를 보여줍니다. 스플리터는 열·행마다 최대 2개까지 생기고 위치는 트랙 수별로 따로 기억해 2열 ↔ 3열을 오갈 때 비율이 흐트러지지 않습니다. 이전 버전의 저장값(단일·좌우·상하·2×2)은 자동으로 새 모드로 이어집니다
- **분할 중 상단바 표기 정리** — 분할 화면에서는 패널마다 `상태 · 프로젝트명 — 세션명 ⎇브랜치` 헤더가 이미 붙으므로 상단바의 같은 표기를 감춥니다(단일 화면은 기존과 동일). 여러 세션의 진행 상태를 모아 보여주는 허가 대기 배지는 계속 표시됩니다

## [0.11.6] - 2026-08-25

### 수정
- **출력 중 스크롤이 바닥으로 되돌아가던 문제** — Codex·Claude 실행 중 스크롤을 위로 올려도 새 출력이 도착할 때마다 맨 아래로 끌려가 진행 중에는 이전 내용을 읽을 수 없었습니다. xterm 이 휠·스크롤바로 인한 스크롤에서는 스크롤 이벤트를 발생시키지 않아 "바닥 추종" 상태가 해제되지 않던 것이 원인으로, 이제 뷰포트 위치를 직접 확인해 판단합니다
- **프리셋 실행 후 키보드 포커스** — 프리셋으로 `/model` 처럼 방향키 선택이 필요한 명령을 실행하면 포커스가 프롬프트 입력창에 남아, 터미널 본문을 한 번 클릭해야 선택할 수 있었습니다. 이제 실행 직후 터미널로 포커스가 이동해 곧바로 방향키로 조작할 수 있습니다. 분할 화면의 패널 프리셋 메뉴도 동일합니다

### 추가 및 개선
- **터미널 가독성 설정** — 설정 창에 줄 간격(1.0~2.0), 자간(0~4px), 최소 대비(1~7) 슬라이더를 추가했습니다. 최소 대비는 Claude·Codex 가 즐겨 쓰는 흐린 회색·dim 출력이 배경에 묻힐 때 글자색만 배경 대비 기준까지 끌어올립니다(1 = 끔, 4.5 = WCAG AA). 슬라이더를 움직이면 열려 있는 모든 터미널에 즉시 반영되고, 취소하거나 Esc 로 닫으면 원래 값으로 되돌아갑니다

## [0.11.5] - 2026-08-25

### 수정
- **프롬프트 입력창 이미지 첨부** — 패널 입력창에서 Cmd/Ctrl+V 로 클립보드 이미지를 붙여넣으면 PNG 로 저장한 경로가 커서 위치에 삽입됩니다(텍스트 붙여넣기는 기존 동작 유지). 탐색기·파일 탐색기에서 끌어온 파일도 입력창 위에 놓으면 그 패널 입력창으로 들어갑니다
- **첨부 이미지 표시 위치** — 창 하단에 고정되던 최근 첨부 스트립이 입력창을 가리던 문제를 고쳤습니다. 이제 각 패널 입력창 바로 위에 그 세션의 첨부 이미지만 표시되고, 스트립이 생기거나 사라질 때 터미널 크기를 다시 맞춥니다

## [0.11.4] - 2026-08-24

### 추가 및 개선
- **패널별 프롬프트 입력창** — 분할 화면에서 각 패널이 자기 세션의 입력창·예약 목록·전송/예약/일괄 버튼을 따로 갖습니다. 패널마다 작성 중인 내용이 독립적으로 유지되고 Cmd/Ctrl+Enter 는 그 패널 세션으로 전송되며, 입력창 높이 상한도 해당 패널 높이의 절반으로 맞췄습니다
- **패널 헤더** — 각 분할 패널 상단에 `상태 · 프로젝트명 — 세션명 ⎇브랜치` 를 프로젝트 색으로 표시해 여러 패널을 띄워도 어느 프로젝트인지 바로 구분됩니다. 진행 상태 태그는 실시간으로 갱신됩니다
- **프리셋 배치 정리** — 전역 프리셋은 상단 고정바에 한 번만 두고(포커스된 패널에 실행), 프로젝트 전용 프리셋은 각 패널 헤더의 드롭다운으로 접어 좁은 패널 폭을 아낍니다. 단일 화면은 기존과 동일
- **사용량 표시 이동** — Codex 남은 사용량과 시스템 메모리 사용률을 우측 패널에서 상단바(분할 아이콘 왼쪽)로 옮겼습니다

## [0.11.3] - 2026-08-24

### 추가 및 개선
- **테마 시스템** — 설정 창에서 다크/라이트 프리셋 12종(macOS·Windows 다크/라이트, 그라파이트, 슬레이트, 다크 퍼플/그린/브라운, 페이퍼·스카이 라이트)을 클릭 한 번으로 적용. 배경색·강조색 두 값만 직접 지정해도 나머지 색이 자동으로 파생되며, 선택한 테마는 재시작 후에도 유지됩니다
- **라이트 테마 가독성 보정** — 배경 밝기로 라이트/다크를 판별해 글자·세션 상태색·프로젝트 색을 자동으로 눌러 칠하고, 코드 하이라이트 테마도 함께 전환. 살아 있는 모든 터미널의 배경·전경·ANSI 팔레트가 즉시 갱신됩니다

## [0.11.2] - 2026-08-24

### 수정
- **macOS 접근 권한 팝업 반복 해결** — 지금까지 macOS 빌드는 서명되지 않아(ad-hoc) macOS 가 권한 기록을 실행 파일의 cdhash 에 고정했고, cdhash 는 릴리즈마다 바뀌므로 "전체 디스크 접근"을 허용해도 업데이트마다 무효화되어 터미널에서 `claude` 실행 시 문서·데스크탑·다운로드·음악·사진 폴더 권한 팝업이 반복해서 떴습니다. 이제 릴리즈 빌드를 고정 인증서로 서명해, 권한 기록이 `번들 ID + 인증서` 기준으로 저장되고 이후 업데이트에도 유지됩니다

> **기존 macOS 사용자 안내 (1회만)**: 이 버전으로 업데이트하면 앱의 서명이 바뀌므로 권한을
> 한 번 다시 허용해야 합니다. 시스템 설정 → 개인정보 보호 및 보안 → 전체 디스크 접근에서
> 기존 `Terminal Assistance` 항목을 `-` 로 제거하고, 앱을 재실행해 다시 허용하세요.
> 이 재허용은 한 번만 필요하며 이후 업데이트에서는 반복되지 않습니다.

## [0.11.0] - 2026-08-24

### 추가 및 개선
- **터미널 분할화면** — 상단바 아이콘으로 단일/좌우/상하/2×2 그리드 뷰 전환. 분할 후 각 패널의 세션 피커에서 원하는 프로젝트 세션을 직접 선택하고, 패널 클릭으로 활성 세션(프롬프트·검색 대상)을 전환. 스플리터 드래그로 비율 조절(20~80%)이 가능하며 분할 모드·패널 배정·비율은 재시작 후 복원. 화면에 보이는 패널 전부에 WebGL 렌더러를 부착해 단일 화면과 동일한 렌더링 성능 유지
- **패널별 프리셋 바** — 분할 중 각 패널 상단에 전역 + 해당 세션 프로젝트의 프리셋 칩을 표시. 클릭하면 그 패널 세션에 실행되고 `{branch}` 등 플레이스홀더도 그 세션 기준으로 치환. 드래그 정렬 지원
- **프리셋 고정 바** — `+ 프리셋` 버튼을 상단바에서 프리셋 바로 이동하고, 프리셋이 없어도 항상 표시되는 고정 바로 변경
- **분할 인식 파일 드롭** — 탐색기 드래그·네이티브 파일 드롭이 커서 아래 패널의 세션으로 삽입되도록 개선

## [0.10.6] - 2026-08-23

### 변경
- **라이선스 전환** — 기존 MIT 라이선스 표기를 GNU General Public License v3.0 only (`GPL-3.0-only`)로 변경하고, 저작권 및 기여자 처리 기준을 명확히 문서화

## [0.10.5] - 2026-08-23

### 추가 및 개선
- **프롬프트 입력 영역** — 전송·예약·일괄 버튼을 세로로 배치해 입력 너비를 넓히고, 입력 내용에 따라 높이가 화면의 최대 50%까지 자동으로 늘어나도록 개선. 전송하면 기본 높이로 복귀
- **프롬프트 전송 단축키** — Enter는 줄바꿈으로 처리하고 Cmd/Ctrl+Enter로 활성 세션에 전송하도록 변경

## [0.10.4] - 2026-08-22

### 추가 및 개선
- **터미널 입력과 스크롤** — 출력 중 위쪽 기록을 읽을 때 자동 하단 이동을 멈추고, 하단으로 돌아오면 최신 출력 추종을 재개. 터미널 아래에 일반 텍스트 입력 기반 프롬프트를 두어 한글·백스페이스 입력을 안정화하고 Enter 전송, Shift+Enter 줄바꿈을 지원
- **프롬프트 작업 흐름** — 실행 중인 세션에 작업을 FIFO로 예약하고 여러 세션에 같은 프롬프트를 일괄 전송하는 기능 추가
- **통합 문서** — 프로젝트의 실제 Markdown 파일로 메모를 저장하고 계획과 함께 열람. 탐색기에서 프로젝트 내부 `.md` 계획 파일을 드롭해 등록할 수 있으며, 계획은 원본을 보존한 채 목록에서 제거하고 메모는 확인 후 파일까지 삭제 가능
- **서식 있는 터미널 복사** — 터미널 선택 영역을 일반 텍스트와 색상·강조 서식이 포함된 HTML로 함께 복사해 메모에 붙여넣을 수 있도록 개선

### 수정
- **런타임 안정성** — 알림 권한 확인과 알림 실패 처리를 보강하고, 문서·세션 목록의 상대시간 공용 함수 누락으로 발생하던 오류 수정

## [0.10.3] - 2026-08-22

### 수정
- **계획 문서 저장** — `+ 선택`을 표준 클릭 경로로 단순화하고, 선택 확인 중 WebKit 프레임 대기나 클립보드 IPC가 멈춰 버튼이 무응답 상태로 남지 않도록 제한 시간을 적용. xterm 선택값을 못 읽는 경우 최근 터미널 드래그 뒤 클립보드 텍스트로 저장하도록 보강

## [0.10.2] - 2026-08-22

### 수정
- **터미널 링크** — xterm 웹링크 클릭을 Tauri opener 경로로 연결해 외부 브라우저가 열리도록 수정
- **터미널 입력** — macOS IME Backspace 처리에서 diff 경로와 폴백 경로가 DEL 을 중복 전송하던 잔여 과삭제 경로 차단
- **터미널 스크롤** — 출력 수신, 복구, 세션 전환, 패널 리사이즈 이후 활성 터미널이 최신 출력으로 내려가도록 보정
- **계획 문서 저장** — 터미널 선택 영역을 클릭 전에 보존하고 저장 성공 항목을 즉시 계획 문서 목록에 반영

## [0.10.1] - 2026-08-22

### 변경
- **세션 패널** — Claude 세션과 Codex 세션을 시간순으로 통합하고 섹션명을 `세션`으로 변경

### 수정
- **커맨드 팔레트** — 검색 패널 위에 마우스가 머물러 있어도 방향키 선택이 동작하고, 실제 마우스 이동 시에만 hover 선택을 반영
- **Codex 한글 입력** — macOS IME 조합 중 Backspace가 이전 글자까지 두 단계로 지우던 경로 보정
- **탐색기 사이드바** — Unity 프로젝트의 `.meta` 파일과 git 상태 항목을 숨겨 파일 목록 노이즈 감소

## [0.10.0] - 2026-08-22

### 추가
- **커맨드 팔레트** — `Cmd/Ctrl+K`로 프로젝트, 세션, 프리셋, 런치 레시피, Claude 세션, 계획 문서를 한 곳에서 검색하고 실행
- **터미널 검색** — `Cmd/Ctrl+F`로 활성 터미널 스크롤백을 검색하고 이전/다음 결과로 이동
- **런치 레시피** — 여러 명령을 여러 터미널 세션으로 한 번에 실행하는 작업 묶음 저장·실행
- **프리셋 변수** — `{branch}`, `{projectPath}`, `{projectName}`, `{session}`, `{clipboard}`, `{input:작업명}` 치환 지원
- **선택 영역 계획 저장** — 터미널에서 드래그한 우선순위·계획 텍스트를 `계획+` / `+ 선택`으로 즉시 계획 문서에 저장

### 수정
- macOS Codex TUI에서 백스페이스 후 재입력 시 이전 텍스트가 간헐적으로 사라지던 문제 완화 — hidden textarea 미러 오염과 IME diff 과삭제 경로 차단

## [0.9.0] - 2026-08-21

### 추가
- **탐색기 사이드바** — 프로젝트 사이드바 옆에 활성 프로젝트의 파일 트리 표시 (폴더 지연 로딩, 펼침 상태 localStorage 유지, 접기/펼치기 폴딩 + 드래그 리사이즈). git 과 연동해 변경 파일에 상태 표시(M 수정 / U 미추적 / A 스테이지, 폴더에는 변경 포함 점) — 5초 주기 갱신
- **트리 드래그로 경로 전달** — 트리 항목을 '다음 프롬프트' 초안 카드나 터미널로 드래그하면 파일 경로가 삽입됨 (공백 경로 자동 따옴표). 특정 파일을 지정해 AI 와 대화할 때 사용
- **파일 미리보기 팝업** — 트리에서 더블클릭 또는 스페이스바(토글: 다시 누르면 닫힘)로 미리보기. 마크다운(marked 렌더링 + 코드 블록 하이라이팅), 코드(highlight.js 컬러), JSON(정렬+하이라이팅), 이미지·비디오·오디오(asset protocol) 지원, 미지원 형식은 [외부 프로그램으로 열기] 안내. 2MB 초과 텍스트는 앞부분만 표시
- **Enter 로 경로 삽입** — 트리에서 파일 선택 후 Enter 를 누르면 활성 터미널 입력 라인 끝에 파일 경로가 삽입됨 (드래그 대체 단축키, 포커스는 트리에 남아 연속 삽입 가능). 폴더는 Enter 로 접기/펼치기
- **트리 방향키 내비게이션** — ↑↓ 선택 이동, → 폴더 펼치기(펼친 폴더에선 첫 자식으로), ← 폴더 접기(파일·닫힌 폴더에선 부모 폴더로) — 일반 코드 에디터와 동일
- **계획 문서 수집 확장** — Claude 전용(jsonl) 추출에 더해 프로젝트 안의 관례적 계획 폴더(`.omc/plans`, `docs/superpowers/specs`, `docs/plans`, `docs/specs`, `.claude/plans`, `plans`)를 직접 스캔. Auto 모드처럼 플랜 모드를 거치지 않고 파일로만 남는 계획도 수집되며, 파일 수정 시 같은 항목이 제자리 갱신됨 (mtime·크기 기반 증분). jsonl Write 규칙도 `/specs/` 경로를 인식하도록 확대. 파일 기반 계획은 목록·팝업에 출처 경로 표시

## [0.8.0] - 2026-08-21

### 추가
- **계획 문서 패널** — Claude 세션 기록에서 플랜 모드 승인 계획(ExitPlanMode)과 plan 성격의 `.md` 작성 내용을 증분 스캔으로 추출해 프로젝트 경로 귀속으로 영속화 (`plans/<경로>.json`). Claude Code 가 옛 세션 기록을 정리해도 계획은 앱에 남아 이후 열람·이어서 진행의 근거가 됨. 리스트 클릭 시 팝업으로 열람
- **Claude 세션 열람 팝업** — 세션 리스트 클릭 시 저장된 대화를 말풍선 팝업으로 표시. 팝업의 [새 터미널에서 이어서 진행] 버튼이 기록 경로와 일치하는 프로젝트에 새 세션을 자동 생성하고 `claude --resume` 을 즉시 실행
- **우측 패널 사용량 헤더** — 패널 최상단에 Codex 남은 사용량(5시간·주간)과 시스템 메모리(`mem N%`)를 함께 표시 (상단 헤더 표기 대체)
- **우측 패널 섹션 폴딩** — Claude 세션·계획 문서·다음 프롬프트 섹션을 접기/펼치기 (상태 localStorage 유지, 기존 높이 분할선 대체)
- **빈 프로젝트 시작 화면** — 세션 없는 프로젝트 클릭 시 메인 중앙에 프로젝트명 + [＋ 새 세션 시작] 버튼 표시

### 변경
- 상단 헤더의 메모리·코덱스 표기를 제거하고 우측 패널 사용량 헤더로 이동
- 세션 없는 프로젝트는 사이드바 폴딩(❯) 아이콘 미표시

### 제거
- 채팅 뷰(터미널/채팅 탭) 제거 — 1초 tail 폴링 등 유지 비용 대비 가치가 낮음. 대화 열람은 Claude 세션 팝업으로 대체

### 수정
- 터미널 입력 중 백스페이스로 지운 뒤 재입력하면 이전 텍스트가 계속 삭제되던 버그(macOS) — 한글 IME diff 가 미러로 쓰는 textarea 에, xterm 이 처리 후 취소하는 백스페이스가 반영되지 않아 미러가 어긋나고 이후 음절 교체 diff 가 이미 지운 글자를 반복 삭제하던 desync 가 원인. 백스페이스(일반·IME 폴백·Cmd/Option)를 미러에도 반영하고, 가로챈 단축키의 `preventDefault` 누락으로 인한 이중 삭제 경로도 차단

## [0.7.0] - 2026-08-20

### 추가
- **허가 대기(🟡) 상태 감지** — AI 도구가 실행 허가를 기다리는 세션을 사이드바·헤더에 표시하고, 비활성 세션이면 데스크톱 알림. Claude Code 는 훅(설정 → AI 도구 연동에서 opt-in 설치, `~/.claude/settings.json` 기존 훅 보존 병합 + 백업), Codex 는 `[tui] notifications` 의 OSC 터미널 알림을 PTY 리더가 직접 가로채는 방식(`~/.codex/config.toml` 병합). 사용자 입력 시 즉시 해제
- **헤더 허가 대기 집계 배지** — 대기 중인 세션 수 표시, 클릭 시 해당 세션으로 점프 (여러 개면 순환)
- **채팅 뷰** — 헤더 [터미널|채팅] 탭으로 메인 화면 전환. 활성 세션의 Claude/Codex 대화 기록 중 최근 활동한 쪽을 자동 선택해 말풍선으로 표시(1초 증분 tail, 도구 호출은 한 줄 축약), 하단 입력창은 PTY 직접 전달. 세션 생성 이전에 멈춘 옛 대화는 표시하지 않음
- **팬아웃** — 다음 프롬프트 초안의 [일괄] 버튼으로 선택한 여러 세션에 같은 프롬프트를 동시 실행 (PTY 직접 전달, 클립보드 미사용)
- **세션 이름** — 프로젝트별 S1, S2… 순번 자동 부여(헤더·알림에는 '프로젝트명 — S2' 형태), 사이드바 더블 클릭으로 인라인 이름 편집 (Enter/포커스 이탈 = 확정, Esc = 취소)
- **완료 배지 확인 추적** — 완료(🔵) 배지를 사용자가 실제로 열람하면 5초 뒤 해제, 끝내 안 보면 5분 폴백 (기존: 세션 전환 즉시 소멸)
- macOS 텍스트 이동 키 — `Cmd+←/→` 줄 시작/끝, `Option+←/→` 단어 이동, `Cmd+⌫` 줄 삭제, `Option+⌫` 단어 삭제 (Home/End 없는 맥북 키보드 대응)

### 수정
- codex 등 TUI 타이핑 시 버벅임과 간헐적 화면 깜빡임 — 출력 코얼레싱을 quiet-gap 방식(출력이 잠잠해질 때까지 수집, 최대 16ms)으로 재설계해 에코 지연을 ~2ms 로 줄이고 재그리기 중간 프레임 노출 제거
- macOS 자동 업데이트 실패 — v0.6.0 릴리즈의 latest.json 서명과 바이너리 불일치(동일 태그 워크플로 중복 실행 경합)를 릴리즈 자산 수정으로 해결하고, 재발 방지용 workflow concurrency 가드 추가

## [0.6.0] - 2026-08-20

### 추가
- Claude 세션 패널: Claude Code 가 자체 저장하는 세션 기록(`~/.claude/projects`)을 현재 세션의 작업 경로 기준으로 나열 (최근 활동 순, 첫 프롬프트 미리보기). 클릭하면 같은 프로젝트에 새 터미널을 열고 `claude --resume <id>` 로 재개
- 헤더에 활성 세션의 git 브랜치 표시 (2초 주기 갱신, 경로는 호버 툴팁으로 이동)
- 헤더에 코덱스(Codex CLI) 사용량 표시 — `~/.codex/sessions` 기록의 rate_limits 를 10초 주기로 읽어 5시간/주간 사용률·리셋 시각 표시 (최근 12시간 내 사용 시에만 노출, 사용률 구간별 색상)
- 접힌 프로젝트에 ＋ 세션을 추가하면 자동으로 펼치며 새 세션 선택

### 변경
- 프리셋 클릭 = 즉시 실행 — 기존 2단계 실행 확인 제거 (Shift+클릭 = 입력만은 유지)
- 다음 프롬프트 [실행] 버튼: 터미널 전달과 동시에 Enter 로 즉시 실행하고, 전송한 초안은 목록에서 제거

### 제거
- 앱 종료 시 세션 스냅샷 저장·재시작 복원 제거 — 복원된 화면 기록의 실효성이 낮아, 작업 재개는 Claude Code 자체 세션 재개로 대체. 구버전이 남긴 스냅샷 파일은 실행 시 자동 정리
- 프롬프트 히스토리 패널·입력 추적 제거 (Claude 세션 목록으로 대체)

### 수정
- 한글 IME 입력이 Windows/macOS 양쪽에서 깨지던 문제(중복·모음 유실·조합 붕괴) — xterm 의 "compositionend 후 textarea substring" 전송이 두 웹뷰 엔진의 이벤트 순서에서 모두 어긋나는 것이 원인.
  `compositionend` 의 커밋 텍스트를 정본으로 즉시 전송하고 xterm 의 지연 전송을 취소하는 방식으로 재작성:
  - WebView2: keyup 이후 지연 도착하는 커밋 `insertText` 를 중복으로 차단
  - WKWebView(macOS) 한글: 실측 결과 조합 이벤트가 전혀 없고 새 음절은 `insertText`, 조합 진행은 `insertReplacementText`(직전 글자 교체)로만 도착 → xterm 이 교체를 무시해 첫 자음만 남던 것("테스트"→"ㅌ스트")을, beforeinput/input 값 diff 로 삭제분은 DEL·삽입분은 그대로 전송해 IME 의 음절 교체를 터미널 라인 편집으로 재현. IME 소비 백스페이스 DEL 폴백, 합성 keypress 중복(xterm.js#5894) 차단 포함
  - 실기기에서 채집한 이벤트 트레이스 포함 9종 재생 하네스로 검증 (실측 "테스트"·백스페이스, 지연 커밋, 조합+공백, 연타, 표준 조합, 합성 keypress, 일반 타이핑, 조합 취소)
- dev 빌드가 설치본 실행 중일 때 시작되지 않던 문제 — 단일 인스턴스 강제를 릴리즈 빌드에만 적용

## [0.5.1] - 2026-08-20

### 수정
- 터미널에서 한글 입력 시 글자가 두 번씩 입력되던 문제 — WebView2 가 IME 조합 커밋 `input` 이벤트를 keyup 이후에 발생시켜 xterm.js 의 중복 방지 가드(`_keyDownSeen`)를 통과, 같은 텍스트가 compositionend 경로와 이중 전송되던 것을 조합 상태 기반 가드로 차단 (이모지 피커·받아쓰기 입력은 영향 없음)

## [0.5.0] - 2026-08-20

### 추가
- 앱 재시작 세션 복원: 앱 종료 시 세션별 스냅샷(작업 폴더·제목·화면 기록·프롬프트 히스토리)을 저장하고, 다음 실행에서 같은 위치에 새 셸을 열어 이전 기록을 그 위에 복원 (구분 배너 표시)
  - 세션을 ✕로 닫으면 스냅샷도 함께 삭제, 7일간 앱 미사용 시 자동 만료
  - 원래 작업 폴더가 사라진 세션은 홈 디렉토리로 폴백 (안내 표시)
  - 강제 종료·크래시 시에는 마지막 정상 종료 시점 내용으로 복원됨
- 단일 인스턴스 강제: 앱을 중복 실행하면 새 창 대신 기존 창을 앞으로 가져옴 (스냅샷 상호 덮어쓰기 방지)
- 사이드바 제목 우측에 앱 버전 표기

### 변경
- 세션 id 를 영속 id 로 변경 — 마지막 활성 세션 기억이 재시작을 넘어 유지됨
- 프롬프트 히스토리가 웹뷰 리로드·앱 재시작 후에도 유지됨 (복원 후 점프는 텍스트 검색으로 동작)

### 수정
- 사이드바 세션 순서가 웹뷰 복구 시 뒤섞이던 문제 (생성 시각 정렬)
- 프롬프트 히스토리 삭제 후 번호 재사용으로 엉뚱한 항목이 삭제될 수 있던 문제

## [0.4.0] - 2026-08-20

### 추가
- 프롬프트 히스토리 항목별 삭제 버튼: ✕ 클릭 → "삭제 확인" 재클릭 2단계 방식
- 세션 닫기(✕)도 동일한 2단계 "삭제 확인" 방식으로 실수 방지
- 현재 활성 세션과 소속 프로젝트를 파랑 배경·좌측 바로 강조 — 보고 있는 위치가 한눈에 구분됨
- 다음 프롬프트 초안을 프롬프트 히스토리 패널 하단 섹션으로 병합 (분할선 드래그로 높이 조절)

### 변경
- 진행 계획(작업 패널) 기능 제거 — 최신 Claude Code 는 todo 파일을 기록하지 않아 자동 연동 데이터 원천이 없음
- 프리셋 칩의 ◆/▸ 아이콘 제거, 텍스트만 표시

### 수정
- 히스토리 클릭 점프가 동작하지 않던 문제: TUI 의 화면 클리어로 마커가 폐기되면 버퍼 텍스트 검색으로 폴백 점프
- 드래그 정렬 후 첫 클릭이 무시되던 문제, 프리셋 관리 팝업의 Esc 리스너 누수, 프롬프트 패널 200~239px 리사이즈 무반응 수정

### 성능·안정성
- 패널 드래그 시 PTY 리사이즈 IPC 폭주 제거 (rAF 코얼레싱 + 치수가 변했을 때만 전송)
- 스크롤백 링버퍼 트리밍 슬랙 도입 — 지속 대량 출력 시 초당 ~120MB 메모리 복사 제거
- 웹뷰 크래시 복구: 스냅샷 병렬 조회 + 복구 중 라이브 큐 1MB 상한 (복구 시점 메모리 스파이크 방지)
- 세션 상태 변경 시 사이드바 전체 재구축 대신 해당 행만 갱신 (호버 깜빡임·드래그 끊김 해소)
- 설정 락 poisoning 시 이후 모든 설정 IPC 가 영구 패닉하던 경로 제거, 저장 실패를 사용자에게 표시

### 내부
- app.js 를 책임별 5개 파일로 분리, 2단계 확인 로직 공용화(ArmedConfirm), 세션 상태 enum 화, CSS 색상 변수화, 죽은 코드 제거

## [0.3.0] - 2026-08-20

### 추가
- 헤더 시스템 메모리 표시기: "메모리 N% 사용중" — 녹색(원활) → 노랑/주황 → 빨강(위험, 점멸) 4단계
- 우측 작업 패널(◧ 작업 버튼, 너비·분할 높이 조절 가능):
  - 상단 진행 계획: Claude Code 작업 계획(~/.claude/todos) 자동 연동 + 완료 자동 체크, 다른 AI 용 수동 항목 추가/체크/삭제
  - 하단 다음 프롬프트: 멀티라인 초안을 여러 개 작성해 두고 [입력] 으로 터미널 입력 라인에 전달 (초안은 프로젝트별 영속화)

### 수정 (안정성 — 대량 출력 시 WebView 크래시 및 세션 내용 소실)
- 출력 파이프라인 3단 방어: 16ms 코얼레싱(IPC 폭주 차단) + ACK 기반 flow control(웹뷰 메모리 상한) + OS 파이프 백프레셔(무한 적체 차단)
- 세션별 스크롤백을 Rust 백엔드에 보관(세션당 2MB) — 웹뷰가 죽어도 내용 유지
- WebView2 렌더러 크래시 시 자동 리로드 + 세션 화면/내용 완전 복원 (PTY·실행 중 작업은 영향 없음)
- xterm WebGL 렌더러 도입(활성 세션에 부착, 미지원 시 DOM 폴백) — 대량 출력 렌더 성능 대폭 개선
- 설정 파일(ta-config.json) 원자적 저장 + 손상 시 .corrupt 백업 (조용한 초기화 방지)
- 뮤텍스 poisoning 전파 차단, 세션별 이미지 목록 상한(12개) 적용

## [0.2.1] - 2026-08-20

### 수정
- 팝업(프로젝트 등록 등)이 바깥 클릭으로 닫혀 작성 내용이 사라지던 문제 — 닫기는 버튼 또는 Esc 로만 동작

## [0.2.0] - 2026-08-20

### 추가
- 우측 프롬프트 히스토리 패널: 입력한 프롬프트 목록, 클릭 시 터미널 해당 위치로 스크롤 점프, 프롬프트 지점 점선 구분선
- 사이드바 프로젝트 폴딩(❯ 아이콘, 접힘 시 미니 상태점 요약) + 포인터 드래그 정렬(파란 점선 인디케이터)
- 좌우 사이드바: 드래그 핸들 너비 조절, 슬라이딩 접기/펼치기(40px 레일)
- 프리셋: 2단계 실행 확인(빨강 "{이름} 실행"), 드래그 정렬, 전역=파랑·맨앞 고정, 관리 팝업(추가/수정/삭제)
- 프로젝트 색상: 12종 프리셋 컬러칩 + 커스텀 칩, 색상은 프로젝트 이름 텍스트에 적용

### 수정
- 한글 입력 깨짐: PTY 에 LANG/LC_ALL(ko_KR.UTF-8) 주입 + IME 조합 중 키 가로채기 방지
- 프로젝트 경로는 목록에서 제거하고 호버 툴팁으로 표시, 설정·＋ 버튼 크기 확대 및 간격 축소

## [0.1.0] - 2026-08-19

### 추가
- 최초 릴리즈 (macOS / Windows)
- 프로젝트 레지스트리: 등록된 프로젝트 클릭 한 번으로 해당 경로 터미널 열기/전환
- 멀티 세션 + 상태 시각화: 실행 중(점멸)/대기/완료(배지·알림)/종료 실시간 표시
- 명령 프리셋: 전역/프로젝트별 작업 지시 등록, 클릭 실행
- 이미지 첨부: 클립보드 붙여넣기(Cmd/Ctrl+V)·드래그앤드롭 → 경로 자동 입력 + 썸네일 히스토리
- 자동 업데이트: 태그 릴리즈 감지 → 앱 내 업데이트 팝업
