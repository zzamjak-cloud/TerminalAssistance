# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.

## [0.10.1] - 2026-08-22

### 변경
- **세션 패널** — Claude 세션과 Codex 세션을 시간순으로 통합하고 섹션명을 `세션`으로 변경

### 수정
- **커맨드 팔레트** — 검색 패널 위에 마우스가 머물러 있어도 방향키 선택이 동작하고, 실제 마우스 이동 시에만 hover 선택을 반영
- **Codex 한글 입력** — macOS IME 조합 중 Backspace가 이전 글자까지 두 단계로 지우던 경로 보정
- **탐색기 사이드바** — Unity 프로젝트의 `.meta` 파일과 git 상태 항목을 숨겨 파일 목록 노이즈 감소

## [0.10.0] - 2026-08-22

### 추가
- **커맨드 팔레트** — `Cmd/Ctrl+K`로 프로젝트, 세션, 프리셋, 런치 레시피, Claude 세션, 계획 문서를 한 곳에서 검색하고 실행
- **터미널 검색** — `Cmd/Ctrl+F`로 활성 터미널 스크롤백을 검색하고 이전/다음 결과로 이동
- **런치 레시피** — 여러 명령을 여러 터미널 세션으로 한 번에 실행하는 작업 묶음 저장·실행
- **프리셋 변수** — `{branch}`, `{projectPath}`, `{projectName}`, `{session}`, `{clipboard}`, `{input:작업명}` 치환 지원
- **선택 영역 계획 저장** — 터미널에서 드래그한 우선순위·계획 텍스트를 `계획+` / `+ 선택`으로 즉시 계획 문서에 저장

### 수정
- macOS Codex TUI에서 백스페이스 후 재입력 시 이전 텍스트가 간헐적으로 사라지던 문제 완화 — hidden textarea 미러 오염과 IME diff 과삭제 경로 차단

## [0.9.0] - 2026-08-21

### 추가
- **탐색기 사이드바** — 프로젝트 사이드바 옆에 활성 프로젝트의 파일 트리 표시 (폴더 지연 로딩, 펼침 상태 localStorage 유지, 접기/펼치기 폴딩 + 드래그 리사이즈). git 과 연동해 변경 파일에 상태 표시(M 수정 / U 미추적 / A 스테이지, 폴더에는 변경 포함 점) — 5초 주기 갱신
- **트리 드래그로 경로 전달** — 트리 항목을 '다음 프롬프트' 초안 카드나 터미널로 드래그하면 파일 경로가 삽입됨 (공백 경로 자동 따옴표). 특정 파일을 지정해 AI 와 대화할 때 사용
- **파일 미리보기 팝업** — 트리에서 더블클릭 또는 스페이스바(토글: 다시 누르면 닫힘)로 미리보기. 마크다운(marked 렌더링 + 코드 블록 하이라이팅), 코드(highlight.js 컬러), JSON(정렬+하이라이팅), 이미지·비디오·오디오(asset protocol) 지원, 미지원 형식은 [외부 프로그램으로 열기] 안내. 2MB 초과 텍스트는 앞부분만 표시
- **Enter 로 경로 삽입** — 트리에서 파일 선택 후 Enter 를 누르면 활성 터미널 입력 라인 끝에 파일 경로가 삽입됨 (드래그 대체 단축키, 포커스는 트리에 남아 연속 삽입 가능). 폴더는 Enter 로 접기/펼치기
- **트리 방향키 내비게이션** — ↑↓ 선택 이동, → 폴더 펼치기(펼친 폴더에선 첫 자식으로), ← 폴더 접기(파일·닫힌 폴더에선 부모 폴더로) — 일반 코드 에디터와 동일
- **계획 문서 수집 확장** — Claude 전용(jsonl) 추출에 더해 프로젝트 안의 관례적 계획 폴더(`.omc/plans`, `docs/superpowers/specs`, `docs/plans`, `docs/specs`, `.claude/plans`, `plans`)를 직접 스캔. Auto 모드처럼 플랜 모드를 거치지 않고 파일로만 남는 계획도 수집되며, 파일 수정 시 같은 항목이 제자리 갱신됨 (mtime·크기 기반 증분). jsonl Write 규칙도 `/specs/` 경로를 인식하도록 확대. 파일 기반 계획은 목록·팝업에 출처 경로 표시

## [0.8.0] - 2026-08-21

### 추가
- **계획 문서 패널** — Claude 세션 기록에서 플랜 모드 승인 계획(ExitPlanMode)과 plan 성격의 `.md` 작성 내용을 증분 스캔으로 추출해 프로젝트 경로 귀속으로 영속화 (`plans/<경로>.json`). Claude Code 가 옛 세션 기록을 정리해도 계획은 앱에 남아 이후 열람·이어서 진행의 근거가 됨. 리스트 클릭 시 팝업으로 열람
- **Claude 세션 열람 팝업** — 세션 리스트 클릭 시 저장된 대화를 말풍선 팝업으로 표시. 팝업의 [새 터미널에서 이어서 진행] 버튼이 기록 경로와 일치하는 프로젝트에 새 세션을 자동 생성하고 `claude --resume` 을 즉시 실행
- **우측 패널 사용량 헤더** — 패널 최상단에 Codex 남은 사용량(5시간·주간)과 시스템 메모리(`mem N%`)를 함께 표시 (상단 헤더 표기 대체)
- **우측 패널 섹션 폴딩** — Claude 세션·계획 문서·다음 프롬프트 섹션을 접기/펼치기 (상태 localStorage 유지, 기존 높이 분할선 대체)
- **빈 프로젝트 시작 화면** — 세션 없는 프로젝트 클릭 시 메인 중앙에 프로젝트명 + [＋ 새 세션 시작] 버튼 표시

### 변경
- 상단 헤더의 메모리·코덱스 표기를 제거하고 우측 패널 사용량 헤더로 이동
- 세션 없는 프로젝트는 사이드바 폴딩(❯) 아이콘 미표시

### 제거
- 채팅 뷰(터미널/채팅 탭) 제거 — 1초 tail 폴링 등 유지 비용 대비 가치가 낮음. 대화 열람은 Claude 세션 팝업으로 대체

### 수정
- 터미널 입력 중 백스페이스로 지운 뒤 재입력하면 이전 텍스트가 계속 삭제되던 버그(macOS) — 한글 IME diff 가 미러로 쓰는 textarea 에, xterm 이 처리 후 취소하는 백스페이스가 반영되지 않아 미러가 어긋나고 이후 음절 교체 diff 가 이미 지운 글자를 반복 삭제하던 desync 가 원인. 백스페이스(일반·IME 폴백·Cmd/Option)를 미러에도 반영하고, 가로챈 단축키의 `preventDefault` 누락으로 인한 이중 삭제 경로도 차단

## [0.7.0] - 2026-08-20

### 추가
- **허가 대기(🟡) 상태 감지** — AI 도구가 실행 허가를 기다리는 세션을 사이드바·헤더에 표시하고, 비활성 세션이면 데스크톱 알림. Claude Code 는 훅(설정 → AI 도구 연동에서 opt-in 설치, `~/.claude/settings.json` 기존 훅 보존 병합 + 백업), Codex 는 `[tui] notifications` 의 OSC 터미널 알림을 PTY 리더가 직접 가로채는 방식(`~/.codex/config.toml` 병합). 사용자 입력 시 즉시 해제
- **헤더 허가 대기 집계 배지** — 대기 중인 세션 수 표시, 클릭 시 해당 세션으로 점프 (여러 개면 순환)
- **채팅 뷰** — 헤더 [터미널|채팅] 탭으로 메인 화면 전환. 활성 세션의 Claude/Codex 대화 기록 중 최근 활동한 쪽을 자동 선택해 말풍선으로 표시(1초 증분 tail, 도구 호출은 한 줄 축약), 하단 입력창은 PTY 직접 전달. 세션 생성 이전에 멈춘 옛 대화는 표시하지 않음
- **팬아웃** — 다음 프롬프트 초안의 [일괄] 버튼으로 선택한 여러 세션에 같은 프롬프트를 동시 실행 (PTY 직접 전달, 클립보드 미사용)
- **세션 이름** — 프로젝트별 S1, S2… 순번 자동 부여(헤더·알림에는 '프로젝트명 — S2' 형태), 사이드바 더블 클릭으로 인라인 이름 편집 (Enter/포커스 이탈 = 확정, Esc = 취소)
- **완료 배지 확인 추적** — 완료(🔵) 배지를 사용자가 실제로 열람하면 5초 뒤 해제, 끝내 안 보면 5분 폴백 (기존: 세션 전환 즉시 소멸)
- macOS 텍스트 이동 키 — `Cmd+←/→` 줄 시작/끝, `Option+←/→` 단어 이동, `Cmd+⌫` 줄 삭제, `Option+⌫` 단어 삭제 (Home/End 없는 맥북 키보드 대응)

### 수정
- codex 등 TUI 타이핑 시 버벅임과 간헐적 화면 깜빡임 — 출력 코얼레싱을 quiet-gap 방식(출력이 잠잠해질 때까지 수집, 최대 16ms)으로 재설계해 에코 지연을 ~2ms 로 줄이고 재그리기 중간 프레임 노출 제거
- macOS 자동 업데이트 실패 — v0.6.0 릴리즈의 latest.json 서명과 바이너리 불일치(동일 태그 워크플로 중복 실행 경합)를 릴리즈 자산 수정으로 해결하고, 재발 방지용 workflow concurrency 가드 추가

## [0.6.0] - 2026-08-20

### 추가
- Claude 세션 패널: Claude Code 가 자체 저장하는 세션 기록(`~/.claude/projects`)을 현재 세션의 작업 경로 기준으로 나열 (최근 활동 순, 첫 프롬프트 미리보기). 클릭하면 같은 프로젝트에 새 터미널을 열고 `claude --resume <id>` 로 재개
- 헤더에 활성 세션의 git 브랜치 표시 (2초 주기 갱신, 경로는 호버 툴팁으로 이동)
- 헤더에 코덱스(Codex CLI) 사용량 표시 — `~/.codex/sessions` 기록의 rate_limits 를 10초 주기로 읽어 5시간/주간 사용률·리셋 시각 표시 (최근 12시간 내 사용 시에만 노출, 사용률 구간별 색상)
- 접힌 프로젝트에 ＋ 세션을 추가하면 자동으로 펼치며 새 세션 선택

### 변경
- 프리셋 클릭 = 즉시 실행 — 기존 2단계 실행 확인 제거 (Shift+클릭 = 입력만은 유지)
- 다음 프롬프트 [실행] 버튼: 터미널 전달과 동시에 Enter 로 즉시 실행하고, 전송한 초안은 목록에서 제거

### 제거
- 앱 종료 시 세션 스냅샷 저장·재시작 복원 제거 — 복원된 화면 기록의 실효성이 낮아, 작업 재개는 Claude Code 자체 세션 재개로 대체. 구버전이 남긴 스냅샷 파일은 실행 시 자동 정리
- 프롬프트 히스토리 패널·입력 추적 제거 (Claude 세션 목록으로 대체)

### 수정
- 한글 IME 입력이 Windows/macOS 양쪽에서 깨지던 문제(중복·모음 유실·조합 붕괴) — xterm 의 "compositionend 후 textarea substring" 전송이 두 웹뷰 엔진의 이벤트 순서에서 모두 어긋나는 것이 원인.
  `compositionend` 의 커밋 텍스트를 정본으로 즉시 전송하고 xterm 의 지연 전송을 취소하는 방식으로 재작성:
  - WebView2: keyup 이후 지연 도착하는 커밋 `insertText` 를 중복으로 차단
  - WKWebView(macOS) 한글: 실측 결과 조합 이벤트가 전혀 없고 새 음절은 `insertText`, 조합 진행은 `insertReplacementText`(직전 글자 교체)로만 도착 → xterm 이 교체를 무시해 첫 자음만 남던 것("테스트"→"ㅌ스트")을, beforeinput/input 값 diff 로 삭제분은 DEL·삽입분은 그대로 전송해 IME 의 음절 교체를 터미널 라인 편집으로 재현. IME 소비 백스페이스 DEL 폴백, 합성 keypress 중복(xterm.js#5894) 차단 포함
  - 실기기에서 채집한 이벤트 트레이스 포함 9종 재생 하네스로 검증 (실측 "테스트"·백스페이스, 지연 커밋, 조합+공백, 연타, 표준 조합, 합성 keypress, 일반 타이핑, 조합 취소)
- dev 빌드가 설치본 실행 중일 때 시작되지 않던 문제 — 단일 인스턴스 강제를 릴리즈 빌드에만 적용

## [0.5.1] - 2026-08-20

### 수정
- 터미널에서 한글 입력 시 글자가 두 번씩 입력되던 문제 — WebView2 가 IME 조합 커밋 `input` 이벤트를 keyup 이후에 발생시켜 xterm.js 의 중복 방지 가드(`_keyDownSeen`)를 통과, 같은 텍스트가 compositionend 경로와 이중 전송되던 것을 조합 상태 기반 가드로 차단 (이모지 피커·받아쓰기 입력은 영향 없음)

## [0.5.0] - 2026-08-20

### 추가
- 앱 재시작 세션 복원: 앱 종료 시 세션별 스냅샷(작업 폴더·제목·화면 기록·프롬프트 히스토리)을 저장하고, 다음 실행에서 같은 위치에 새 셸을 열어 이전 기록을 그 위에 복원 (구분 배너 표시)
  - 세션을 ✕로 닫으면 스냅샷도 함께 삭제, 7일간 앱 미사용 시 자동 만료
  - 원래 작업 폴더가 사라진 세션은 홈 디렉토리로 폴백 (안내 표시)
  - 강제 종료·크래시 시에는 마지막 정상 종료 시점 내용으로 복원됨
- 단일 인스턴스 강제: 앱을 중복 실행하면 새 창 대신 기존 창을 앞으로 가져옴 (스냅샷 상호 덮어쓰기 방지)
- 사이드바 제목 우측에 앱 버전 표기

### 변경
- 세션 id 를 영속 id 로 변경 — 마지막 활성 세션 기억이 재시작을 넘어 유지됨
- 프롬프트 히스토리가 웹뷰 리로드·앱 재시작 후에도 유지됨 (복원 후 점프는 텍스트 검색으로 동작)

### 수정
- 사이드바 세션 순서가 웹뷰 복구 시 뒤섞이던 문제 (생성 시각 정렬)
- 프롬프트 히스토리 삭제 후 번호 재사용으로 엉뚱한 항목이 삭제될 수 있던 문제

## [0.4.0] - 2026-08-20

### 추가
- 프롬프트 히스토리 항목별 삭제 버튼: ✕ 클릭 → "삭제 확인" 재클릭 2단계 방식
- 세션 닫기(✕)도 동일한 2단계 "삭제 확인" 방식으로 실수 방지
- 현재 활성 세션과 소속 프로젝트를 파랑 배경·좌측 바로 강조 — 보고 있는 위치가 한눈에 구분됨
- 다음 프롬프트 초안을 프롬프트 히스토리 패널 하단 섹션으로 병합 (분할선 드래그로 높이 조절)

### 변경
- 진행 계획(작업 패널) 기능 제거 — 최신 Claude Code 는 todo 파일을 기록하지 않아 자동 연동 데이터 원천이 없음
- 프리셋 칩의 ◆/▸ 아이콘 제거, 텍스트만 표시

### 수정
- 히스토리 클릭 점프가 동작하지 않던 문제: TUI 의 화면 클리어로 마커가 폐기되면 버퍼 텍스트 검색으로 폴백 점프
- 드래그 정렬 후 첫 클릭이 무시되던 문제, 프리셋 관리 팝업의 Esc 리스너 누수, 프롬프트 패널 200~239px 리사이즈 무반응 수정

### 성능·안정성
- 패널 드래그 시 PTY 리사이즈 IPC 폭주 제거 (rAF 코얼레싱 + 치수가 변했을 때만 전송)
- 스크롤백 링버퍼 트리밍 슬랙 도입 — 지속 대량 출력 시 초당 ~120MB 메모리 복사 제거
- 웹뷰 크래시 복구: 스냅샷 병렬 조회 + 복구 중 라이브 큐 1MB 상한 (복구 시점 메모리 스파이크 방지)
- 세션 상태 변경 시 사이드바 전체 재구축 대신 해당 행만 갱신 (호버 깜빡임·드래그 끊김 해소)
- 설정 락 poisoning 시 이후 모든 설정 IPC 가 영구 패닉하던 경로 제거, 저장 실패를 사용자에게 표시

### 내부
- app.js 를 책임별 5개 파일로 분리, 2단계 확인 로직 공용화(ArmedConfirm), 세션 상태 enum 화, CSS 색상 변수화, 죽은 코드 제거

## [0.3.0] - 2026-08-20

### 추가
- 헤더 시스템 메모리 표시기: "메모리 N% 사용중" — 녹색(원활) → 노랑/주황 → 빨강(위험, 점멸) 4단계
- 우측 작업 패널(◧ 작업 버튼, 너비·분할 높이 조절 가능):
  - 상단 진행 계획: Claude Code 작업 계획(~/.claude/todos) 자동 연동 + 완료 자동 체크, 다른 AI 용 수동 항목 추가/체크/삭제
  - 하단 다음 프롬프트: 멀티라인 초안을 여러 개 작성해 두고 [입력] 으로 터미널 입력 라인에 전달 (초안은 프로젝트별 영속화)

### 수정 (안정성 — 대량 출력 시 WebView 크래시 및 세션 내용 소실)
- 출력 파이프라인 3단 방어: 16ms 코얼레싱(IPC 폭주 차단) + ACK 기반 flow control(웹뷰 메모리 상한) + OS 파이프 백프레셔(무한 적체 차단)
- 세션별 스크롤백을 Rust 백엔드에 보관(세션당 2MB) — 웹뷰가 죽어도 내용 유지
- WebView2 렌더러 크래시 시 자동 리로드 + 세션 화면/내용 완전 복원 (PTY·실행 중 작업은 영향 없음)
- xterm WebGL 렌더러 도입(활성 세션에 부착, 미지원 시 DOM 폴백) — 대량 출력 렌더 성능 대폭 개선
- 설정 파일(ta-config.json) 원자적 저장 + 손상 시 .corrupt 백업 (조용한 초기화 방지)
- 뮤텍스 poisoning 전파 차단, 세션별 이미지 목록 상한(12개) 적용

## [0.2.1] - 2026-08-20

### 수정
- 팝업(프로젝트 등록 등)이 바깥 클릭으로 닫혀 작성 내용이 사라지던 문제 — 닫기는 버튼 또는 Esc 로만 동작

## [0.2.0] - 2026-08-20

### 추가
- 우측 프롬프트 히스토리 패널: 입력한 프롬프트 목록, 클릭 시 터미널 해당 위치로 스크롤 점프, 프롬프트 지점 점선 구분선
- 사이드바 프로젝트 폴딩(❯ 아이콘, 접힘 시 미니 상태점 요약) + 포인터 드래그 정렬(파란 점선 인디케이터)
- 좌우 사이드바: 드래그 핸들 너비 조절, 슬라이딩 접기/펼치기(40px 레일)
- 프리셋: 2단계 실행 확인(빨강 "{이름} 실행"), 드래그 정렬, 전역=파랑·맨앞 고정, 관리 팝업(추가/수정/삭제)
- 프로젝트 색상: 12종 프리셋 컬러칩 + 커스텀 칩, 색상은 프로젝트 이름 텍스트에 적용

### 수정
- 한글 입력 깨짐: PTY 에 LANG/LC_ALL(ko_KR.UTF-8) 주입 + IME 조합 중 키 가로채기 방지
- 프로젝트 경로는 목록에서 제거하고 호버 툴팁으로 표시, 설정·＋ 버튼 크기 확대 및 간격 축소

## [0.1.0] - 2026-08-19

### 추가
- 최초 릴리즈 (macOS / Windows)
- 프로젝트 레지스트리: 등록된 프로젝트 클릭 한 번으로 해당 경로 터미널 열기/전환
- 멀티 세션 + 상태 시각화: 실행 중(점멸)/대기/완료(배지·알림)/종료 실시간 표시
- 명령 프리셋: 전역/프로젝트별 작업 지시 등록, 클릭 실행
- 이미지 첨부: 클립보드 붙여넣기(Cmd/Ctrl+V)·드래그앤드롭 → 경로 자동 입력 + 썸네일 히스토리
- 자동 업데이트: 태그 릴리즈 감지 → 앱 내 업데이트 팝업
