# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.

## [0.4.0] - 2026-08-20

### 추가
- 프롬프트 히스토리 항목별 삭제 버튼: ✕ 클릭 → "삭제 확인" 재클릭 2단계 방식
- 세션 닫기(✕)도 동일한 2단계 "삭제 확인" 방식으로 실수 방지
- 현재 활성 세션과 소속 프로젝트를 파랑 배경·좌측 바로 강조 — 보고 있는 위치가 한눈에 구분됨
- 다음 프롬프트 초안을 프롬프트 히스토리 패널 하단 섹션으로 병합 (분할선 드래그로 높이 조절)

### 변경
- 진행 계획(작업 패널) 기능 제거 — 최신 Claude Code 는 todo 파일을 기록하지 않아 자동 연동 데이터 원천이 없음
- 프리셋 칩의 ◆/▸ 아이콘 제거, 텍스트만 표시

### 수정
- 히스토리 클릭 점프가 동작하지 않던 문제: TUI 의 화면 클리어로 마커가 폐기되면 버퍼 텍스트 검색으로 폴백 점프
- 드래그 정렬 후 첫 클릭이 무시되던 문제, 프리셋 관리 팝업의 Esc 리스너 누수, 프롬프트 패널 200~239px 리사이즈 무반응 수정

### 성능·안정성
- 패널 드래그 시 PTY 리사이즈 IPC 폭주 제거 (rAF 코얼레싱 + 치수가 변했을 때만 전송)
- 스크롤백 링버퍼 트리밍 슬랙 도입 — 지속 대량 출력 시 초당 ~120MB 메모리 복사 제거
- 웹뷰 크래시 복구: 스냅샷 병렬 조회 + 복구 중 라이브 큐 1MB 상한 (복구 시점 메모리 스파이크 방지)
- 세션 상태 변경 시 사이드바 전체 재구축 대신 해당 행만 갱신 (호버 깜빡임·드래그 끊김 해소)
- 설정 락 poisoning 시 이후 모든 설정 IPC 가 영구 패닉하던 경로 제거, 저장 실패를 사용자에게 표시

### 내부
- app.js 를 책임별 5개 파일로 분리, 2단계 확인 로직 공용화(ArmedConfirm), 세션 상태 enum 화, CSS 색상 변수화, 죽은 코드 제거

## [0.3.0] - 2026-08-20

### 추가
- 헤더 시스템 메모리 표시기: "메모리 N% 사용중" — 녹색(원활) → 노랑/주황 → 빨강(위험, 점멸) 4단계
- 우측 작업 패널(◧ 작업 버튼, 너비·분할 높이 조절 가능):
  - 상단 진행 계획: Claude Code 작업 계획(~/.claude/todos) 자동 연동 + 완료 자동 체크, 다른 AI 용 수동 항목 추가/체크/삭제
  - 하단 다음 프롬프트: 멀티라인 초안을 여러 개 작성해 두고 [입력] 으로 터미널 입력 라인에 전달 (초안은 프로젝트별 영속화)

### 수정 (안정성 — 대량 출력 시 WebView 크래시 및 세션 내용 소실)
- 출력 파이프라인 3단 방어: 16ms 코얼레싱(IPC 폭주 차단) + ACK 기반 flow control(웹뷰 메모리 상한) + OS 파이프 백프레셔(무한 적체 차단)
- 세션별 스크롤백을 Rust 백엔드에 보관(세션당 2MB) — 웹뷰가 죽어도 내용 유지
- WebView2 렌더러 크래시 시 자동 리로드 + 세션 화면/내용 완전 복원 (PTY·실행 중 작업은 영향 없음)
- xterm WebGL 렌더러 도입(활성 세션에 부착, 미지원 시 DOM 폴백) — 대량 출력 렌더 성능 대폭 개선
- 설정 파일(ta-config.json) 원자적 저장 + 손상 시 .corrupt 백업 (조용한 초기화 방지)
- 뮤텍스 poisoning 전파 차단, 세션별 이미지 목록 상한(12개) 적용

## [0.2.1] - 2026-08-20

### 수정
- 팝업(프로젝트 등록 등)이 바깥 클릭으로 닫혀 작성 내용이 사라지던 문제 — 닫기는 버튼 또는 Esc 로만 동작

## [0.2.0] - 2026-08-20

### 추가
- 우측 프롬프트 히스토리 패널: 입력한 프롬프트 목록, 클릭 시 터미널 해당 위치로 스크롤 점프, 프롬프트 지점 점선 구분선
- 사이드바 프로젝트 폴딩(❯ 아이콘, 접힘 시 미니 상태점 요약) + 포인터 드래그 정렬(파란 점선 인디케이터)
- 좌우 사이드바: 드래그 핸들 너비 조절, 슬라이딩 접기/펼치기(40px 레일)
- 프리셋: 2단계 실행 확인(빨강 "{이름} 실행"), 드래그 정렬, 전역=파랑·맨앞 고정, 관리 팝업(추가/수정/삭제)
- 프로젝트 색상: 12종 프리셋 컬러칩 + 커스텀 칩, 색상은 프로젝트 이름 텍스트에 적용

### 수정
- 한글 입력 깨짐: PTY 에 LANG/LC_ALL(ko_KR.UTF-8) 주입 + IME 조합 중 키 가로채기 방지
- 프로젝트 경로는 목록에서 제거하고 호버 툴팁으로 표시, 설정·＋ 버튼 크기 확대 및 간격 축소

## [0.1.0] - 2026-08-19

### 추가
- 최초 릴리즈 (macOS / Windows)
- 프로젝트 레지스트리: 등록된 프로젝트 클릭 한 번으로 해당 경로 터미널 열기/전환
- 멀티 세션 + 상태 시각화: 실행 중(점멸)/대기/완료(배지·알림)/종료 실시간 표시
- 명령 프리셋: 전역/프로젝트별 작업 지시 등록, 클릭 실행
- 이미지 첨부: 클립보드 붙여넣기(Cmd/Ctrl+V)·드래그앤드롭 → 경로 자동 입력 + 썸네일 히스토리
- 자동 업데이트: 태그 릴리즈 감지 → 앱 내 업데이트 팝업
